begin

  if (Trees['status'] <> NULL) and (Trees_old['status_old'] <> NULL) then begin
  
    if (Trees['status'] in [1]) and (Trees_old['status_old'] in [0]) then begin
      showwarning('Tree was dead in the previous census. Please double-check.');
    end;
    
  end;

end.